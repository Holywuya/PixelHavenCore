# 耐久显示模块配置文件
# 通过 PacketEvents 拦截发包，在可损坏物品的 lore 最下方显示耐久信息。
# 需要安装 PacketEvents 插件作为前置。

# 是否启用
enabled: true

# Lore 格式
# 占位符:
#   {current} - 当前耐久（最大耐久 - 已损坏值）
#   {max}     - 最大耐久
#   {color}   - 自动颜色（根据耐久百分比: >50% 绿色, >25% 黄色, >10% 红色, <=10% 深红色）
loreFormat: "&7耐久: {color}{current}&7/&c{max}"
